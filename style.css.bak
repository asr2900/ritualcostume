/* -----------------------------------------------------------
   DESIGN TOKENS — magic dark green x turquoise
----------------------------------------------------------- */
:root {
  --ink: #071a15;
  --ink-raised: #0e2a22;
  --white: #f4fffb;
  --white-dim: #9fc9bd;
  --black: #05100c;
  --teal: #2ddac0;
  --teal-deep: #12876f;
  --teal-soft: rgba(45, 218, 192, 0.12);

  --font-display: "Baloo 2", sans-serif;
  --font-body: "Quicksand", sans-serif;
  --radius: 14px;
}

* { box-sizing: border-box; }

html, body {
  margin: 0;
  padding: 0;
  background: var(--ink);
  color: var(--white);
  font-family: var(--font-body);
  min-height: 100vh;
}

[hidden] { display: none !important; }

.ritual-bg-noise {
  position: fixed;
  inset: 0;
  pointer-events: none;
  background-image:
    radial-gradient(circle at 15% 8%, rgba(45, 218, 192, 0.10), transparent 42%),
    radial-gradient(circle at 88% 85%, rgba(18, 135, 111, 0.16), transparent 48%);
  z-index: 0;
}

/* -----------------------------------------------------------
   HEADER
----------------------------------------------------------- */
.site-header {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  gap: 0.7rem;
  padding: 1.4rem clamp(1.2rem, 4vw, 3rem) 1rem;
  border-bottom: 1px solid rgba(244, 255, 251, 0.08);
}

.logo-frame {
  width: 34px;
  height: 34px;
  border-radius: 50%;
  overflow: hidden;
  flex-shrink: 0;
  background: var(--ink-raised);
  border: 1px solid rgba(45, 218, 192, 0.4);
}
.logo-img { width: 100%; height: 100%; object-fit: cover; display: block; }

.wordmark {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 1.4rem;
  color: var(--white);
}
.wordmark-thin { font-weight: 500; color: var(--teal); }

.tagline {
  font-family: var(--font-body);
  font-weight: 600;
  font-size: 0.78rem;
  color: var(--white-dim);
  margin-left: auto;
  text-transform: lowercase;
  letter-spacing: 0.03em;
}

.back-link {
  font-family: var(--font-display);
  font-weight: 600;
  font-size: 0.85rem;
  color: var(--white-dim);
  text-decoration: none;
  transition: color 0.15s ease;
}
.back-link:hover { color: var(--teal); }

/* -----------------------------------------------------------
   LAYOUT (builder)
----------------------------------------------------------- */
.lab {
  position: relative;
  z-index: 1;
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
  gap: clamp(1.5rem, 3vw, 3rem);
  padding: clamp(1.5rem, 4vw, 3rem);
  max-width: 1200px;
  margin: 0 auto;
}
@media (max-width: 860px) { .lab { grid-template-columns: 1fr; } }

/* -----------------------------------------------------------
   STAGE — preview avatar KOTAK (tanpa kucing)
----------------------------------------------------------- */
.stage {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.2rem;
}

.stage-frame {
  position: relative;
  width: min(100%, 420px);
  aspect-ratio: 1 / 1;
}

/* Frame kotak mengikuti bentuk asli gambar */
#avatarCanvas {
  width: 100%;
  height: 100%;
  border-radius: var(--radius);
  background: var(--ink-raised);
  border: 1px solid rgba(45, 218, 192, 0.3);
  box-shadow:
    0 0 0 1px rgba(244, 255, 251, 0.08),
    0 0 40px rgba(45, 218, 192, 0.15),
    0 20px 60px rgba(0, 0, 0, 0.5);
}

.stage-actions { display: flex; gap: 0.7rem; flex-wrap: wrap; justify-content: center; }

.stage-hint {
  font-size: 0.78rem;
  color: var(--white-dim);
  text-align: center;
  margin: 0;
}

/* -----------------------------------------------------------
   BUTTONS
----------------------------------------------------------- */
.btn {
  font-family: var(--font-display);
  font-size: 0.85rem;
  font-weight: 700;
  padding: 0.7rem 1.3rem;
  border-radius: 999px;
  border: 1px solid transparent;
  cursor: pointer;
  transition: transform 0.15s ease, opacity 0.15s ease;
}
.btn:hover { transform: translateY(-1px) scale(1.02); }
.btn:active { transform: translateY(0); }
.btn:disabled { cursor: not-allowed; opacity: 0.5; transform: none; }

.btn-primary {
  background: linear-gradient(135deg, var(--teal), var(--teal-deep));
  color: var(--black);
}
.btn-ghost {
  background: transparent;
  border-color: rgba(244, 255, 251, 0.25);
  color: var(--white);
}

/* -----------------------------------------------------------
   CONTROLS (tabs + swatch strip)
----------------------------------------------------------- */
.controls { display: flex; flex-direction: column; gap: 1rem; }

.tabs { display: flex; flex-wrap: wrap; gap: 0.5rem; }

.tab-btn {
  font-family: var(--font-display);
  font-weight: 600;
  font-size: 0.85rem;
  color: var(--white-dim);
  background: var(--ink-raised);
  border: 1px solid rgba(244, 255, 251, 0.1);
  border-radius: 999px;
  padding: 0.5rem 1.1rem;
  cursor: pointer;
  transition: all 0.15s ease;
}
.tab-btn:hover { color: var(--white); border-color: rgba(45, 218, 192, 0.4); }
.tab-btn.active {
  color: var(--black);
  background: linear-gradient(135deg, var(--teal), var(--teal-deep));
  border-color: transparent;
}

.swatch-scroll-wrap { display: flex; align-items: center; gap: 0.5rem; }

.swatch-strip {
  display: flex;
  gap: 0.7rem;
  overflow-x: auto;
  scroll-behavior: smooth;
  padding: 0.3rem 0.1rem 0.7rem;
  scrollbar-width: thin;
  scrollbar-color: var(--teal-deep) transparent;
}
.swatch-strip::-webkit-scrollbar { height: 6px; }
.swatch-strip::-webkit-scrollbar-thumb { background: var(--teal-deep); border-radius: 999px; }

.scroll-btn {
  flex-shrink: 0;
  width: 32px; height: 32px;
  border-radius: 50%;
  border: 1px solid rgba(244, 255, 251, 0.15);
  background: var(--ink-raised);
  color: var(--white);
  font-size: 1.1rem;
  line-height: 1;
  cursor: pointer;
}
.scroll-btn:hover { border-color: var(--teal); color: var(--teal); }

.swatch-col {
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.35rem;
}

.swatch {
  width: 64px; height: 64px;
  border-radius: var(--radius);
  border: 2px solid rgba(244, 255, 251, 0.12);
  background: var(--ink-raised);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  padding: 0;
  transition: border-color 0.15s ease, transform 0.15s ease;
  flex-shrink: 0;
}
.swatch:hover { transform: translateY(-2px); }
.swatch img { width: 100%; height: 100%; object-fit: cover; }
.swatch.empty { font-family: var(--font-display); font-size: 1.3rem; color: var(--white-dim); }
.swatch.active { border-color: var(--teal); box-shadow: 0 0 0 3px var(--teal-soft); }

.swatch-name {
  font-size: 0.65rem;
  color: var(--white-dim);
  max-width: 70px;
  text-align: center;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.load-status {
  display: none;
  font-family: var(--font-body);
  font-size: 0.75rem;
  line-height: 1.5;
  color: #ffd9a0;
  background: rgba(255, 217, 160, 0.08);
  border: 1px solid rgba(255, 217, 160, 0.3);
  border-radius: var(--radius);
  padding: 0.6rem 0.9rem;
  margin: 0;
}
.load-status.visible { display: block; }
.load-status code {
  background: rgba(0, 0, 0, 0.25);
  padding: 0.05rem 0.35rem;
  border-radius: 4px;
  font-size: 0.72rem;
}

/* -----------------------------------------------------------
   IDENTITY PAGE (sub-web)
----------------------------------------------------------- */
.identity-stage {
  position: relative;
  z-index: 1;
  max-width: 560px;
  margin: 0 auto;
  padding: clamp(1.5rem, 4vw, 3rem) 1.2rem 4rem;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.identity-card {
  width: 100%;
  background: var(--ink-raised);
  border: 1px solid rgba(45, 218, 192, 0.25);
  border-radius: var(--radius);
  padding: 1.6rem;
}

.identity-avatar-preview {
  width: 120px; height: 120px;
  margin: 0 auto 1.2rem;
  border-radius: var(--radius);
  overflow: hidden;
  border: 1px solid rgba(45, 218, 192, 0.35);
  background: var(--ink);
}
.identity-avatar-preview img { width: 100%; height: 100%; object-fit: cover; display: block; }

.identity-eyebrow {
  font-family: var(--font-display);
  font-weight: 700;
  font-size: 1.05rem;
  color: var(--teal);
  text-align: center;
  margin: 0 0 1.2rem;
}

.identity-label {
  display: block;
  font-family: var(--font-body);
  font-weight: 600;
  font-size: 0.82rem;
  color: var(--white-dim);
  margin: 0.9rem 0 0.4rem;
}

.identity-textarea {
  width: 100%;
  resize: vertical;
  min-height: 56px;
  font-family: var(--font-body);
  font-size: 0.9rem;
  color: var(--white);
  background: var(--ink);
  border: 1px solid rgba(244, 255, 251, 0.15);
  border-radius: 10px;
  padding: 0.6rem 0.7rem;
  transition: border-color 0.15s ease;
}
.identity-textarea:focus { outline: none; border-color: var(--teal); }
.identity-textarea.field-error { border-color: var(--teal); box-shadow: 0 0 0 3px var(--teal-soft); }

.identity-note { font-size: 0.75rem; color: var(--white-dim); margin: 0.9rem 0 0; line-height: 1.5; }
.identity-submit { width: 100%; margin-top: 1.2rem; }

/* ---- Animasi magic (loading) ---- */
.identity-loading {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.4rem;
  padding: 3rem 0;
  text-align: center;
  font-family: var(--font-body);
  font-size: 0.9rem;
  color: var(--white-dim);
}
.magic-orb { position: relative; width: 96px; height: 96px; }
.magic-ring, .magic-core { position: absolute; border-radius: 50%; }
.magic-ring {
  inset: 0;
  border: 2px dashed rgba(45, 218, 192, 0.6);
  animation: magic-spin 3s linear infinite;
}
.magic-ring:nth-child(2) {
  inset: 16px;
  border-style: solid;
  border-color: rgba(45, 218, 192, 0.35);
  animation: magic-spin 2s linear infinite reverse;
}
.magic-core {
  inset: 34px;
  background: radial-gradient(circle, var(--teal), var(--teal-deep));
  box-shadow: 0 0 24px rgba(45, 218, 192, 0.8);
  animation: magic-pulse 1.2s ease-in-out infinite;
}
@keyframes magic-spin { to { transform: rotate(360deg); } }
@keyframes magic-pulse {
  0%, 100% { transform: scale(0.8); opacity: 0.7; }
  50% { transform: scale(1.15); opacity: 1; }
}

.identity-error-view {
  text-align: center;
  font-family: var(--font-body);
  font-size: 0.9rem;
  color: var(--white-dim);
  padding: 2rem 0;
}
.identity-error-view p { margin: 0 0 1rem; }

/* ---- Kartu hasil bertema magic ---- */
.identity-result { width: 100%; }
.identity-result.card-reveal .ritual-card { animation: card-in 0.7s cubic-bezier(0.16, 1, 0.3, 1); }
@keyframes card-in {
  0% { opacity: 0; transform: translateY(24px) scale(0.96); }
  100% { opacity: 1; transform: none; }
}

.ritual-card {
  position: relative;
  overflow: hidden;
  background:
    radial-gradient(circle at 50% 0%, rgba(45, 218, 192, 0.18), transparent 60%),
    linear-gradient(160deg, var(--ink-raised), var(--black));
  border: 1px solid rgba(45, 218, 192, 0.4);
  border-radius: 20px;
  padding: 1.6rem;
  text-align: center;
  box-shadow: 0 0 50px rgba(45, 218, 192, 0.18), 0 24px 60px rgba(0, 0, 0, 0.55);
}
.ritual-card-glow {
  position: absolute;
  inset: -40%;
  background: conic-gradient(from 0deg, transparent, rgba(45, 218, 192, 0.12), transparent 40%);
  animation: magic-spin 8s linear infinite;
  pointer-events: none;
}
.ritual-card-pfp {
  position: relative;
  width: 180px; height: 180px;
  margin: 0 auto 1.2rem;
  border-radius: var(--radius);
  overflow: hidden;
  border: 2px solid var(--teal);
  box-shadow: 0 0 30px rgba(45, 218, 192, 0.5);
}
.ritual-card-pfp img { width: 100%; height: 100%; object-fit: cover; display: block; }

.identity-label-small {
  position: relative;
  font-family: var(--font-body);
  font-weight: 600;
  font-size: 0.7rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--white-dim);
  margin: 0 0 0.3rem;
}
.identity-name {
  position: relative;
  font-family: var(--font-display);
  font-weight: 800;
  font-size: 1.6rem;
  color: var(--teal);
  margin: 0 0 1rem;
  line-height: 1.25;
  text-shadow: 0 0 18px rgba(45, 218, 192, 0.5);
}
.identity-desc {
  position: relative;
  font-family: var(--font-body);
  font-size: 0.92rem;
  color: var(--white);
  line-height: 1.6;
  margin: 0;
}

.identity-result-actions { display: flex; gap: 0.6rem; margin-top: 1rem; }
.identity-result-actions .btn { flex: 1; }

/* -----------------------------------------------------------
   FOOTER
----------------------------------------------------------- */
.site-footer {
  position: relative;
  z-index: 1;
  text-align: center;
  padding: 2rem 1rem 3rem;
  font-family: var(--font-body);
  font-weight: 600;
  font-size: 0.78rem;
  color: var(--white-dim);
}

/* Hormati preferensi reduce-motion */
@media (prefers-reduced-motion: reduce) {
  .magic-ring, .magic-core, .ritual-card-glow,
  .identity-result.card-reveal .ritual-card { animation: none !important; }
}
